using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface Movement
{
    // Start is called before the first frame update
    abstract void PerformMovement();
   
}
